<!DOCTYPE html>
<html lang="en">
<head>
<%- include('../components/header') %>
</head>
<body>
  <%- include('../components/nav') %>

  <main class="px-10 mt-12 gap-x-3">
    <h1 class="text-3xl text-center">add todo</h1>
    <form method="post" class="flex flex-col w-1/2 gap-4 p-5 mx-auto border border-red-400 rounded-lg">
      <div>
        <label for="category">Category</label>
        <input value="<%= todo.category_name %>" type="text" name="category" id="category" class="px-3 py-1 text-red-400 border border-red-400 rounded-lg outline-none placeholder:text-red-400" placeholder="category">
      </div>
      
      <div>
        <label for="text">Text</label>
        <input value="<%= todo.name %>" type="text" name="text" id="text" class="px-3 py-1 text-red-400 border border-red-400 rounded-lg outline-none placeholder:text-red-400" placeholder="text">
      </div>
  
      <div class="">
        <button type="submit" class="px-3 py-1 text-white bg-green-600 border border-green-700 rounded-lg w-max">Kaydet</button>
        <a href="/" class="px-3 py-1 text-white bg-blue-600 border border-blue-700 rounded-lg w-max">geri dön</a>
      </div>
    </form>
  </main>

</body>
</html>